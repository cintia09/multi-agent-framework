# Sample task notes

We discovered that the deploy.sh script must be invoked with `--region us-east-1`
otherwise the lambda upload fails with a cryptic 502. Document this for future
tasks so we don't re-discover it.
