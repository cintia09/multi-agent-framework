# Secret regex patterns for sec-audit (one per line, python regex).
# Lines starting with # are comments.
# Derived from implementation-v6.md §M2.3 secret-scan set.

# OpenAI / Anthropic style (broadened to cover modern key formats:
# sk-proj-* project keys and sk-ant-api03-* Anthropic API v3 keys).
sk-(?:proj-)?[A-Za-z0-9_-]{20,}
sk-ant-(?:api03-)?[A-Za-z0-9_-]{20,}

# AWS access key
AKIA[0-9A-Z]{16}

# GitHub tokens
ghp_[A-Za-z0-9]{36}
gho_[A-Za-z0-9]{36}
github_pat_[A-Za-z0-9_]{82}

# Private keys
-----BEGIN (RSA|OPENSSH|DSA|EC|PGP) PRIVATE KEY-----
